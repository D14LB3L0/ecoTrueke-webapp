export interface IProduct {
  id: string;
  productPicture: string | null;
  name: string;
  quantity: number;
  description: string;
  typeTranscription: string;
  category: string;
  condition: string;
}
