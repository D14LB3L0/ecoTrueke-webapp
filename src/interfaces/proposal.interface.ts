export interface IProposal {
  id: string;
  proposerId: string;
  ownerId: string;
  proposalType: string;
  offeredProductId: string;
  requestedProductId: string;
  status: string;
  createdAt: string;
}
